class RenameService:
    def __init__(self):
        self.patterns = []
    
    def rename_files(self, files, pattern):
        """Renombrar archivos según patrón"""
        print(f"Renombrando {len(files)} archivos con patrón: {pattern}")
        return []
    
    def preview_rename(self, files, pattern):
        """Vista previa del renombrado"""
        print(f"Vista previa para {len(files)} archivos")
        return []